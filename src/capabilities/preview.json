{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "preview-capability",
  "description": "The preview webview intentionally has no application command permissions.",
  "webviews": ["preview"],
  "permissions": []
}
