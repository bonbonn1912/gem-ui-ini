{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions for the main GeminUI window.",
  "webviews": ["main"],
  "permissions": ["core:default", "main-commands"]
}
